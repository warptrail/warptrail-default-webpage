const greeting = "Greetings traveler. Welcome to the Web.";
function displayMessage(message) {
    console.log(message);
}

displayMessage(greeting);

// reduce gives you an accumulator and a value in your callback function

